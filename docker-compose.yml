services:
  api:
    container_name: webgis-api
    build: .
    ports:
      - "4431:4431"
    environment:
      - NODE_ENV=development
    volumes:
      - ./:/app
      - /app/node_modules
    command: npm run dev # use npm start for production


